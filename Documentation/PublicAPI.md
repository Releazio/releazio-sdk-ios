# Complete Public API Reference

Полный список всех публичных методов Releazio SDK, доступных для интеграторов.

## Releazio.shared - Все публичные методы

### ✅ Основные методы (рекомендуются для использования)

#### 1. `configure(with:)` - Статический метод
```swift
public static func configure(with configuration: ReleazioConfiguration)
```
**Назначение:** Настройка SDK при запуске приложения  
**Использование:** Обязателен, должен вызываться первым

#### 2. `checkUpdates()` - Главный метод
```swift
public func checkUpdates() async throws -> UpdateState
```
**Назначение:** Основной метод для проверки обновлений  
**Возвращает:** Полное состояние обновлений (`UpdateState`)  
**Использование:** ✅ **ОСНОВНОЙ МЕТОД** - используйте его для проверки обновлений

#### 3. `openAppStore(updateState:)` - Открытие App Store
```swift
public func openAppStore(updateState: UpdateState) -> Bool
```
**Назначение:** Открывает App Store для обновления приложения  
**Возвращает:** `true` если URL открыт успешно  
**Использование:** ✅ Используйте при клике на кнопку "Обновить"

#### 4. `openPostURL(updateState:)` - Открытие поста
```swift
public func openPostURL(updateState: UpdateState) -> Bool
```
**Назначение:** Открывает URL поста (при клике на бейдж или кнопку "?")  
**Возвращает:** `true` если URL открыт успешно  
**Примечание:** Автоматически отмечает пост как открытый для `updateType == 0`  
**Использование:** ✅ Используйте при клике на бейдж или кнопку информации

#### 5. `markPostAsOpened(postURL:)` - Отметить пост как открытый
```swift
public func markPostAsOpened(postURL: String)
```
**Назначение:** Вручную отметить пост как открытый (для `updateType == 0`)  
**Использование:** ⚠️ **ОПЦИОНАЛЬНО** - `openPostURL()` делает это автоматически, но можно вызвать вручную если нужно

#### 6. `markPopupAsShown(version:updateType:)` - Отметить попап как показанный
```swift
public func markPopupAsShown(version: String, updateType: Int)
```
**Назначение:** Отметить попап как показанный (для логики `show_interval` типов 2, 3)  
**Использование:** ✅ **ВАЖНО** - вызывайте когда показываете попап типов 2 или 3, чтобы SDK учитывал интервал между показами

#### 7. `skipUpdate(version:)` - Пропустить обновление
```swift
public func skipUpdate(version: String) -> Int
```
**Назначение:** Пропустить обновление и уменьшить счетчик пропусков (для `updateType == 3`)  
**Возвращает:** Оставшееся количество пропусков  
**Использование:** ✅ Используйте для типа 3 при клике на "Пропустить"

### ⚠️ Устаревшие методы (deprecated - не рекомендуется к использованию)

#### 8. `checkForUpdates()` - ⚠️ DEPRECATED
```swift
@available(*, deprecated, message: "Use checkUpdates() instead...")
public func checkForUpdates() async throws -> Bool
```
**Назначение:** Проверка обновлений (старая версия)  
**Возвращает:** Только `Bool` - есть обновление или нет  
**Проблема:** ❌ Не возвращает полную информацию о состоянии  
**Статус:** ⚠️ **DEPRECATED** - будет отмечен предупреждением компилятора  
**Рекомендация:** Используйте `checkUpdates()` вместо этого

#### 9. `getReleases()` - ⚠️ DEPRECATED
```swift
@available(*, deprecated, message: "Use checkUpdates() to get update state...")
public func getReleases() async throws -> [Release]
```
**Назначение:** Получить все релизы приложения  
**Статус:** ⚠️ **DEPRECATED** - будет отмечен предупреждением компилятора  
**Рекомендация:** Используйте `checkUpdates()` для получения состояния обновлений с полными данными

#### 10. `getLatestRelease()` - ⚠️ DEPRECATED
```swift
@available(*, deprecated, message: "Use checkUpdates() instead...")
public func getLatestRelease() async throws -> Release?
```
**Назначение:** Получить информацию о последнем релизе  
**Статус:** ⚠️ **DEPRECATED** - будет отмечен предупреждением компилятора  
**Рекомендация:** Используйте `checkUpdates()` вместо этого - он предоставляет полную информацию о типе обновления и состоянии

#### 11. `getChangelog(for:)` - Получить changelog
```swift
public func getChangelog(for releaseId: String) async throws -> Changelog
```
**Назначение:** Получить changelog для конкретного релиза  
**Использование:** ⚠️ Может быть полезен, но обычно changelog доступен через `post_url` из `UpdateState`

#### 12. `getConfig()` - Получить конфигурацию API
```swift
public func getConfig() async throws -> ConfigResponse
```
**Назначение:** Получить полную конфигурацию из API напрямую  
**Использование:** ⚠️ Внутренний метод, обычно используется `checkUpdates()` который уже вызывает его

### 🔧 Вспомогательные методы

#### 13. `getConfiguration()` - Получить текущую конфигурацию
```swift
public func getConfiguration() -> ReleazioConfiguration?
```
**Назначение:** Получить текущую конфигурацию SDK  
**Возвращает:** Конфигурацию или `nil` если не настроена  
**Использование:** ⚠️ Может быть полезен для проверки настроек

#### 14. `reset()` - Сброс SDK
```swift
public func reset()
```
**Назначение:** Сбросить конфигурацию и очистить кэш  
**Использование:** ⚠️ Используйте только для тестирования или если нужно полностью переинициализировать SDK

#### 15. `showUpdatePrompt()` - ⚠️ DEPRECATED
```swift
@available(*, deprecated, message: "This method is not implemented. Use UI components directly...")
public func showUpdatePrompt()
```
**Назначение:** Показать попап обновления (заглушка)  
**Проблема:** ❌ **НЕ РЕАЛИЗОВАН** - только трекает аналитику, не показывает UI  
**Статус:** ⚠️ **DEPRECATED** - будет отмечен предупреждением компилятора  
**Рекомендация:** Не используйте! Используйте UI компоненты напрямую (`ReleazioUpdatePromptView` или `ReleazioUpdatePromptViewController`)

## Рекомендуемый workflow

### Правильная последовательность использования:

```swift
// 1. Настройка (при запуске приложения)
Releazio.configure(with: configuration)

// 2. Проверка обновлений (периодически или при необходимости)
let updateState = try await Releazio.shared.checkUpdates()

// 3. Отображение UI в зависимости от типа обновления
switch updateState.updateType {
case 0: // latest
    if updateState.shouldShowBadge {
        // Показать BadgeView
        // При клике: Releazio.shared.openPostURL(updateState: updateState)
    }
    
case 1: // silent
    if updateState.shouldShowUpdateButton {
        // Показать кнопку обновления
        // При клике: Releazio.shared.openAppStore(updateState: updateState)
    }
    
case 2: // popup
    if updateState.shouldShowPopup {
        // Показать ReleazioUpdatePromptView
        // При закрытии: Releazio.shared.markPopupAsShown(...)
        // При обновлении: Releazio.shared.openAppStore(updateState: updateState)
    }
    
case 3: // popup force
    if updateState.shouldShowPopup {
        // Показать ReleazioUpdatePromptView
        // При пропуске: Releazio.shared.skipUpdate(version: ...)
        // При обновлении: Releazio.shared.openAppStore(updateState: updateState)
    }
}
```

## Что НЕ нужно вызывать

❌ **Не используйте:**
- `showUpdatePrompt()` - не реализован, только аналитика
- `checkForUpdates()` (старый метод) - используйте `checkUpdates()` вместо него
- Прямой вызов `getConfig()` - это делается внутри `checkUpdates()`
- `markPostAsOpened()` если используете `openPostURL()` - делается автоматически

✅ **Обязательно используйте:**
- `checkUpdates()` - для проверки обновлений
- `markPopupAsShown()` - когда показываете попап типов 2 или 3
- `skipUpdate()` - при пропуске обновления типа 3

## Резюме публичных методов

| Метод | Статус | Использование |
|-------|--------|---------------|
| `configure(with:)` | ✅ Обязателен | При запуске приложения |
| `checkUpdates()` | ✅ Основной | Для проверки обновлений |
| `openAppStore()` | ✅ Важен | При обновлении |
| `openPostURL()` | ✅ Важен | При открытии поста |
| `markPopupAsShown()` | ✅ Важен | При показе попапа |
| `skipUpdate()` | ✅ Важен | При пропуске (тип 3) |
| `markPostAsOpened()` | ⚠️ Опционально | Если не используете `openPostURL()` |
| `getConfig()` | ⚠️ Внутренний | Обычно не нужен |
| `getConfiguration()` | ⚠️ Опционально | Для проверки настроек |
| `reset()` | ⚠️ Для тестирования | Редко нужен |
| `checkForUpdates()` | ⚠️ **DEPRECATED** | Используйте `checkUpdates()` |
| `getReleases()` | ⚠️ **DEPRECATED** | Используйте `checkUpdates()` |
| `getLatestRelease()` | ⚠️ **DEPRECATED** | Используйте `checkUpdates()` |
| `getChangelog()` | ⚠️ Дополнительный | Не основной workflow |
| `showUpdatePrompt()` | ⚠️ **DEPRECATED** | Используйте UI компоненты напрямую |

---

**Вывод:** Метод `markPopupAsShown()` в документации используется правильно - он публичный и нужен для правильной работы логики `show_interval` для типов обновлений 2 и 3.

